# تقرير الفجوات في نظام إدارة المشاريع - مكتب المنارة الهندسي

## تاريخ التقرير: {{ date('Y-m-d') }}

---

## 1. الموظفين وبياناتهم

### ✅ موجود:
- إضافة/تعديل/حذف موظف
- رقم الهوية (`national_id`)
- الوظيفة (`job_title`)
- شهادة مزاولة المهنة - رقم (`practice_license_no`)
- تاريخ انتهاء تصنيف المهندس (`engineer_rank_expiry`)
- تحديد صلاحيات الموظفين حسب الـRole
- ربط المهام المسندة لكل موظف

### ❌ ناقص:
1. **رفع ملف شهادة مزاولة المهنة** - لا يوجد حقل لرفع ملف PDF/صورة للشهادة
2. **فصل رقم الأرض عن كود الأرض** - حالياً `land_number` واحد فقط، المتطلبات تذكر "رقم الأرض" و"كود الأرض" كحقلين منفصلين

---

## 2. المشاريع وبياناتها

### ✅ موجود:
- تسجيل مشروع جديد بجميع البيانات الأساسية
- أنواع المشاريع (تصميم، تصميم وإشراف، إشراف، تقرير فني، تقرير دفاع مدني، تصميم دفاع مدني، تعديلات، استشارات)
- المراحل (معماري، إنشائي، كهربائي، ميكانيكي، تقديم للبلدية، أخرى)
- Third Party (اسم + تاريخ)
- رقم طلب معاملات منصة بلدي (`baladi_request_number`)
- رفع مرفقات (العقد، المخطط، رسومات هندسية)
- رقم العقد (`contract_number`)
- ملف العقد (`contract_file`)
- رقم/كود الأرض (`land_number`)
- المخطط (`plan_file`)

### ❌ ناقص:
1. **فصل رقم الأرض عن كود الأرض** - يحتاج حقلين منفصلين:
   - `land_number` (رقم الأرض)
   - `land_code` (كود الأرض)
2. **المراحل السبعة** - حالياً يوجد 6 مراحل فقط:
   - معماري ✓
   - إنشائي ✓
   - كهربائي ✓
   - ميكانيكي ✓
   - تقديم للبلدية ✓
   - أخرى ✓
   - **ناقص: المرحلة السابعة** (يجب تحديدها)

---

## 3. إدارة المهام (Tasks Management)

### ✅ موجود:
- إسناد التاسكات للمهندسين والفنيين
- فلترة (من/إلى التاريخ – العميل – المشروع)
- مدير المشروع/مدير المكتب يقدر يعدل أو يرفض التاسك
- إضافة ملاحظات من مدير المشروع
- متابعة حالة كل تاسك: جديد – قيد التنفيذ – منجز – مرفوض
- رفع مرفقات للمهام
- تعليقات على المهام

### ❌ ناقص:
- **لا توجد مشاكل واضحة** - الوحدة مكتملة نسبياً

---

## 4. لوحة الإدارة (Admin Panel)

### ✅ موجود:
- إدارة المستخدمين والأدوار
- التحكم في صلاحيات الوصول
- إدارة الإعدادات العامة

### ❌ ناقص:
- **لا توجد مشاكل واضحة** - الوحدة مكتملة

---

## 5. الإعدادات العامة

### ✅ موجود:
- إعدادات النظام: شعار، لغة، تنسيقات البريد
- بيانات الدعم الفني

### ❌ ناقص:
1. **إعدادات الحسابات والدفعات** - لا يوجد نظام لإعدادات الحسابات (مثل: عدد الدفعات الافتراضي، تنسيق الفواتير، إلخ)

---

## 6. العملاء (Clients)

### ✅ موجود:
- واجهات عرض العملاء (`clients/index.blade.php`, `clients/show.blade.php`, `clients/create.blade.php`, `clients/edit.blade.php`)
- مودال لرفع المرفقات (`client-attachment.blade.php`)
- مودال للملاحظات (`client-notes.blade.php`)

### ❌ ناقص:
1. **Client Model** - لا يوجد `app/Models/Client.php`
2. **Migration للعملاء** - لا يوجد جدول `clients` في قاعدة البيانات
3. **تطبيق منطق العملاء** - `ClientsController` موجود لكن معظمه `TODO`
4. **نظام موافقات العملاء** - لا يوجد نظام لتتبع موافقات/رفض/ملاحظات العميل على المراحل
5. **ربط العملاء بالمشاريع** - لا يوجد علاقة بين `clients` و `projects`

---

## 7. الحسابات/الفواتير/الدفعات (Financials)

### ✅ موجود:
- واجهات عرض الفواتير (`financials/index.blade.php`, `financials/create.blade.php`, `financials/show.blade.php`, `financials/edit.blade.php`)
- مودال الدفعات (`payment-modal.blade.php`)
- Routes للفواتير والدفعات

### ❌ ناقص:
1. **Invoice Model** - لا يوجد `app/Models/Invoice.php`
2. **Payment Model** - لا يوجد `app/Models/Payment.php`
3. **Migration للفواتير** - لا يوجد جدول `invoices` في قاعدة البيانات
4. **Migration للدفعات** - لا يوجد جدول `payments` في قاعدة البيانات
5. **تطبيق منطق الفواتير** - `FinancialsController` موجود لكن معظمه `TODO`:
   - `store()` - TODO
   - `update()` - TODO
   - `destroy()` - TODO
   - `generatePdf()` - TODO
   - `storePayment()` - TODO
   - `updatePaymentStatus()` - TODO
6. **توليد PDF للفواتير** - لا يوجد تطبيق
7. **تحديد عدد الدفعات** - لا يوجد نظام لتحديد عدد الدفعات لكل مشروع
8. **تسجيل كل دفعة** - لا يوجد نظام لتسجيل: رقم الدفعة، قيمتها، تاريخها، ملاحظات
9. **إصدار فواتير للأطراف الثالثة** - لا يوجد نظام لإصدار فواتير بما تم تحصيله من المبلغ المُحصل للأطراف الثالثة
10. **ربط الفواتير بالمشاريع** - لا يوجد علاقة بين `invoices` و `projects`

---

## 8. دورة حياة المشروع (Workflow)

### ✅ موجود:
- بداية المشروع: تسجيل العقد وبيانات المشروع
- المراحل: نظام المراحل موجود
- المهام: نظام المهام موجود
- المرفقات: رفع المرفقات موجود

### ❌ ناقص:

#### أ) المرحلة المعمارية:
1. **رفع الرسم من المهندس المعماري** - لا يوجد نظام محدد لرفع الرسومات المعمارية
2. **مراجعة العميل وإرسال ملاحظاته** - لا يوجد نظام لموافقات/ملاحظات العميل
3. **تحديد مدة التنفيذ بناءً على ملاحظات العميل** - لا يوجد نظام لتحديد المدة بناءً على الملاحظات

#### ب) المرحلة الإنشائية:
1. **موافقة العميل النهائية** - لا يوجد نظام لتتبع موافقة العميل
2. **طلب موافقة مدير المشروع** - لا يوجد نظام محدد لطلب الموافقة

#### ج) المرحلة الكهربائية والميكانيكية:
1. **إرسال المخططات** - لا يوجد نظام محدد لإرسال المخططات
2. **المتابعة عبر التاسكات** - موجود جزئياً

#### د) متابعة التنفيذ:
1. **إرسال ملاحظات مباشرة للفريق** - موجود جزئياً (ملاحظات المدير موجودة)
2. **إمكانية تعديل أو رفض الأعمال المنفذة** - موجود جزئياً

#### هـ) مرحلة البلدية:
1. **رفع أوراق التقديم للبلدية** - لا يوجد نظام محدد لرفع أوراق التقديم
2. **إصدار رخصة البناء** - لا يوجد حقل/نظام لرفع ملف رخصة البناء
3. **رفع رخصة البناء على النظام** - لا يوجد نظام لرفع الرخصة

#### و) الحسابات:
1. **تسجيل قيمة المشروع** - موجود ✓
2. **تحديد عدد الدفعات** - ناقص ❌
3. **متابعة حالة كل دفعة** - ناقص ❌
4. **إصدار PDF لكل دفعة** - ناقص ❌

---

## 9. نظام الموافقات والملاحظات من العميل

### ❌ ناقص بالكامل:
1. **نظام موافقات العميل** - لا يوجد:
   - موافقة على المرحلة المعمارية
   - موافقة على المرحلة الإنشائية
   - موافقة على المراحل الأخرى
2. **نظام ملاحظات العميل** - لا يوجد:
   - إرسال ملاحظات على المراحل
   - متابعة الملاحظات
   - تحديد مدة التنفيذ بناءً على الملاحظات
3. **نظام رفض العميل** - لا يوجد:
   - رفض المرحلة
   - سبب الرفض
   - إعادة العمل بعد الرفض

---

## 10. نظام رفع الملفات المتخصصة

### ✅ موجود:
- رفع ملف العقد
- رفع المخطط
- رفع المرفقات العامة

### ❌ ناقص:
1. **رفع الرسومات المعمارية** - لا يوجد نظام محدد
2. **رفع الرسومات الإنشائية** - لا يوجد نظام محدد
3. **رفع الرسومات الكهربائية** - لا يوجد نظام محدد
4. **رفع الرسومات الميكانيكية** - لا يوجد نظام محدد
5. **رفع أوراق التقديم للبلدية** - لا يوجد نظام محدد
6. **رفع رخصة البناء** - لا يوجد حقل/نظام محدد
7. **رفع ملف شهادة مزاولة المهنة للموظف** - لا يوجد

---

## ملخص الفجوات الرئيسية:

### 🔴 حرجة (يجب إكمالها):
1. **نظام العملاء (Clients)** - Model, Migration, Controller Logic
2. **نظام الفواتير والدفعات (Invoices & Payments)** - Models, Migrations, Controller Logic, PDF Generation
3. **نظام موافقات العميل** - Approval System للعملاء
4. **رفع رخصة البناء** - Building Permit Upload
5. **فصل رقم الأرض عن كود الأرض** - Separate Fields

### 🟡 مهمة (يُنصح بإكمالها):
1. **تحديد عدد الدفعات للمشروع** - Payment Count System
2. **تحديد مدة التنفيذ بناءً على ملاحظات العميل** - Duration Calculation
3. **نظام رفع الملفات المتخصصة** - Specialized File Uploads
4. **المرحلة السابعة** - تحديد المرحلة السابعة المفقودة
5. **إعدادات الحسابات** - Financial Settings

### 🟢 تحسينات (اختيارية):
1. **تحسين نظام المتابعة** - Enhanced Tracking
2. **تقارير متقدمة** - Advanced Reports
3. **إشعارات** - Notifications System

---

## التوصيات:

1. **أولوية عالية**: إكمال نظام العملاء والفواتير والدفعات
2. **أولوية متوسطة**: إضافة نظام موافقات العميل
3. **أولوية منخفضة**: تحسينات إضافية

---

**تم إعداد التقرير بواسطة:** AI Assistant  
**التاريخ:** {{ date('Y-m-d H:i:s') }}




